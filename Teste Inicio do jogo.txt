<!DOCTYPE html> 
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Entrada do Jogo</title>
  <style>
    body, html {
      margin: 0;
      padding: 0;
      height: 100%;
      overflow: hidden;
      font-family: Arial, sans-serif;
    }

    .background {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url('https://i.postimg.cc/0yQ5TTgJ/1.gif') no-repeat center center fixed;
      background-size: cover;
      z-index: -1;
      filter: brightness(1.0);
    }

    .fade-in {
      animation: fadeIn 2s ease-in;
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    .container {
      position: relative;
      text-align: center;
      color: white;
      height: 100vh; /* altura total para usar position absolute dentro */
    }

    /* Botão com posição absoluta */
    #playerButton, #menu {
      position: absolute;
      left: 50%;
      top: 80%; /* Mesmo top para os dois */
      transform: translateX(-50%);
      z-index: 10;
    }

    #playerButton {
      padding: 15px 40px;
      font-size: 1.5em;
      border: none;
      border-radius: 30px;
      background-color: #FFD700;
      color: #000;
      cursor: pointer;
      animation: pulse 1.5s infinite;
      transition: transform 0.3s ease;
    }

    #playerButton:hover {
      transform: translateX(-50%) scale(1.1);
    }

    @keyframes pulse {
      0% { box-shadow: 0 0 0 0 rgba(255, 215, 0, 0.7); }
      70% { box-shadow: 0 0 0 15px rgba(255, 215, 0, 0); }
      100% { box-shadow: 0 0 0 0 rgba(255, 215, 0, 0); }
    }

    /* Menu fica oculto até ser ativado */
    #menu {
      display: none; /* escondido inicialmente */
    }

    /* Estilo dos botões do menu */
    #menu button {
      padding: 15px 40px;
      margin: 10px 5px;
      font-size: 1.5em;
      border: none;
      border-radius: 30px;
      background-color: #add8e6;
      color: #333;
      cursor: pointer;
      transition: background-color 0.3s ease;
      display: inline-block;
      min-width: 150px;
    }

    #menu button:hover {
      background-color: #cceeff;
    }

    .fade-out {
      animation: fadeOut 1s forwards;
    }

    @keyframes fadeOut {
      to { opacity: 0; transform: scale(1.1); }
    }

    #muteButton {
      position: fixed;
      top: 20px;
      right: 20px;
      background-color: rgba(255, 255, 255, 0.7);
      color: black;
      border: none;
      border-radius: 50%;
      width: 50px;
      height: 50px;
      font-size: 1.5em;
      cursor: pointer;
      z-index: 1000;
      transition: background-color 0.3s ease;
    }

    #muteButton:hover {
      background-color: rgba(255, 255, 255, 1);
    }

    /* Modal de instruções */
    #modal {
      display: none;
      position: fixed;
      z-index: 2000;
      left: 0; top: 0;
      width: 100%; height: 100%;
      background-color: rgba(0,0,0,0.7);
      justify-content: center;
      align-items: center;
    }

    #modalContent {
      background-color: white;
      padding: 20px 30px;
      border-radius: 10px;
      max-width: 400px;
      color: black;
      font-size: 1.2em;
      text-align: left;
      position: relative;
    }

    #closeModal {
      position: absolute;
      top: 10px; right: 10px;
      background: #ccc;
      border: none;
      font-size: 1.5em;
      border-radius: 50%;
      width: 30px; height: 30px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  
  <!-- Música de fundo -->
  <audio id="bgMusic" src="https://raw.githubusercontent.com/kellynobrega/Musicas/main/1.mp3" loop></audio>

  <!-- Fundo animado -->
  <div class="background"></div>

  <!-- Botão de Mudo -->
  <button id="muteButton" title="Ativar/Desativar som">🔇</button>

  <!-- Conteúdo principal -->
  <div class="container fade-in" id="mainContainer">
    <button id="playerButton" class="animated-button">Start Game</button>

    <div id="menu">
      <button id="playButton">▶ Jogar</button>
      <button id="instructionsButton">ℹ Instruções</button>
    </div>
  </div>

  <!-- Modal de instruções -->
  <div id="modal">
    <div id="modalContent">
      <button id="closeModal" title="Fechar">×</button>
      <h2>Instruções do Jogo</h2>
      <p>🎮 Instruções:</p>
      <ol>
        <li>Clique em Jogar.</li>
        <li>Ao entrar no jogo, escolha uma das 8 imagens.</li>
        <li>Pinte a imagem como quiser.</li>
        <li>Salve ou recomece quando terminar.</li>
      </ol>
    </div>
  </div>

  <script>
    window.addEventListener('DOMContentLoaded', () => {
      const bgMusic = document.getElementById('bgMusic');
      const muteButton = document.getElementById('muteButton');
      const playerButton = document.getElementById('playerButton');
      const menu = document.getElementById('menu');
      const container = document.getElementById('mainContainer');
      const modal = document.getElementById('modal');
      const closeModal = document.getElementById('closeModal');
      const playButton = document.getElementById('playButton');
      const instructionsButton = document.getElementById('instructionsButton');

      // Tenta iniciar a música automaticamente
      function startMusic() {
        bgMusic.volume = 1;
        bgMusic.play().catch((e) => {
          console.warn("Autoplay com som pode estar bloqueado:", e);
        });
      }
      setTimeout(startMusic, 500);

      function updateMuteIcon() {
        muteButton.textContent = bgMusic.muted || bgMusic.volume === 0 ? '🔇' : '🔊';
      }

      muteButton.addEventListener('click', () => {
        bgMusic.muted = !bgMusic.muted;
        updateMuteIcon();
      });

      playerButton.addEventListener('click', () => {
        menu.style.display = 'block';
        playerButton.style.display = 'none';
        bgMusic.pause();
      });

      playButton.addEventListener('click', () => {
  window.location.href = "https://sites.google.com/view/cantinhoimaginario/jogos/animais-coloridos/menu";
      });




      instructionsButton.addEventListener('click', () => {
        modal.style.display = 'flex';
      });

      closeModal.addEventListener('click', () => {
        modal.style.display = 'none';
      });

      // Fechar modal clicando fora do conteúdo
      modal.addEventListener('click', (e) => {
        if(e.target === modal) {
          modal.style.display = 'none';
        }
      });

      updateMuteIcon();
    });
  </script>
</body>
</html>
